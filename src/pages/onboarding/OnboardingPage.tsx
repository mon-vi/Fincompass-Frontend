import { SectionHeader } from '@/components/ui/SectionHeader';
import { Card } from '@/components/ui/Card';

/** Placeholder — full implementation in Phase 2 (Onboarding feature). */
export function OnboardingPage() {
  return (
    <div className="space-y-6">
      <SectionHeader title="Welcome to FinCompass" subtitle="Let's get you set up" />
      <Card>
        <p className="text-sm text-slate-500">Onboarding flow — Phase 2</p>
      </Card>
    </div>
  );
}
