import { Card } from '@/components/ui/Card';
import { SectionHeader } from '@/components/ui/SectionHeader';

/** Placeholder — full implementation in Phase 2 (Auth feature). */
export function RegisterPage() {
  return (
    <Card>
      <SectionHeader title="Create account" subtitle="Phase 2 — Auth feature" />
    </Card>
  );
}
