import { Card } from '@/components/ui/Card';
import { SectionHeader } from '@/components/ui/SectionHeader';

/** Placeholder — full implementation in Phase 2 (Auth feature). */
export function LoginPage() {
  return (
    <Card>
      <SectionHeader title="Sign in" subtitle="Phase 2 — Auth feature" />
    </Card>
  );
}
